spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/english_trainer
    username: trainer_user
    password: secure_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect 